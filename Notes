Linux Compilation
A la fin bat avec un gcc et un ./ qui compile et exe le code entier


QST//
laisse les commentaires de code

run les unit tests dans un autre thread?

ReadPage Utiliser usigned char* au lieu de byte vu qu'on a pas d'autres truc?

faire mes unit tests en CPP? OUI

Modifier DBConfig pour ajouter le nombre par defaut de fichiers pour les PageID
Modifier PagId

cmake -B cmake-build-debug .;
cd cmake-build-debug;
make;
OU
cmake -B cmake-build-debug .;
cd cmake-build-debug;
make SGDB;